# Nginx configuration for quiz.germanwakad.click
# Copy to /etc/nginx/sites-available/quiz and symlink to sites-enabled

server {
    listen 80;
    server_name quiz.germanwakad.click;

    # Redirect HTTP to HTTPS (uncomment after SSL setup)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://127.0.0.1:5001;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static {
        alias /var/www/quiz/app/static;
        expires 1d;
    }
}

# HTTPS configuration (uncomment after running certbot)
# server {
#     listen 443 ssl;
#     server_name quiz.germanwakad.click;
#
#     ssl_certificate /etc/letsencrypt/live/quiz.germanwakad.click/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/quiz.germanwakad.click/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:5001;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /static {
#         alias /var/www/quiz/app/static;
#         expires 1d;
#     }
# }
